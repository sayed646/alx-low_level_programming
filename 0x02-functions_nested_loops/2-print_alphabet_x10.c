#include "main.h"

/**
 * main - prints 10 times the alphabet, in lowercase, followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
 int a = 0;
    char letter = 'a';
    while(a < 10)
    while(letter <= 'z')
    {
    _putchar(letter);
    letter++;
    {
       _putchar('\n');
        a++;
    }
    }
}
    return (0);
}
