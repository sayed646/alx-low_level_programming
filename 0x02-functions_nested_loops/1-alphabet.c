#include "main.h"

/**
 * main -  prints the alphabet, in lowercase, followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	char small = 'a';
	while(small <= 'z') 

    _putchar(samll);

	small++;
    return (0);
}
