#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void){
	for(int a = 0; int b < 10; a += 1);
	for(int a = 0; int b < 10; b += 1);
	Console.WriteLine("{0}{1}", firstDigit, secondDigit);
}
